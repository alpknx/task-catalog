spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/task_catalog
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  flyway:
    enabled: true
    locations: classpath:db/migration
